//For of loop is similar to regular for loops but it makes it more readable
//Note: For of loop is used in arrays

const arr = [1,2,3,4,5];

//syntax of for of loop
for (const iterator of arr) {
    console.log(iterator);
}